# v0.4 Agent Context

This milestone adds the deterministic retrieval layer for agent context.

User request → relevant PRD requirements → candidate files/tests → relevant drift findings.

The output is `.groundwork/context.json` and is explicitly marked `candidate_context`. Semantic interpretation and LLM-assisted context synthesis remain future layers.
